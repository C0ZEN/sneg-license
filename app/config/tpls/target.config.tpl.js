/**
 * jscs:disable validateQuoteMarks
 */
(function (angular) {
    'use strict';

    angular
        .module('snegLicense')
        .constant('config'/* @if true **, /* @endif *//* @echo config */);

}(window.angular));